pipeline {
    agent { label 'work_station'}
    options {
        disableConcurrentBuilds()
        ansiColor('xterm')
    }
    stages
    {
        stage ('git clone')
        environment {
            NEWRELIC_API_KEY = credentials('ec2_private_key')
        }
        {
            steps
            {
            sh 'hostname'
            git branch: 'main', url: 'https://github.com/ChaitanyaChandra/ansible-lab.git'
            dir('ansible-lab') {
            }
            }
         }

        stage ('ansible ping')
        {
            steps
            {
            sh 'ansible -m ping all -i roboshop.inv'
            sh 'echo ${NEWRELIC_API_KEY} | base64'
            }
        }
    }
}
